import { DefaultTheme } from 'styled-components';
import mixin from './mixin';

const theme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },

  

mixin
};

export { theme };